<?xml version="1.0" encoding="utf-8"?>
<!-- One image, drawn at the widget's real pixel size in ReadinessWidget.draw().
     No nested layout: RemoteViews cannot measure or adapt, and every fixed dp
     here is another way to clip on a device with different cell sizes. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg">

    <ImageView
        android:id="@+id/widget_days"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:contentDescription="@string/widget_description" />
</FrameLayout>
